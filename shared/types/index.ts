export type User = {
  id: string,
  firstName: string,
  lastName: string,
  email: string,
  phoneNumber: string,
  company: string,
}

export type Pagination = {
  page: number,
  limit: number,
}
